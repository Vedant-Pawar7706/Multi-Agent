version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./mytrip.db
      - JWT_SECRET=mytrip-super-secret-jwt-key-change-in-production-2026
      - LLM_PROVIDER=demo
    volumes:
      - ./backend/mytrip.db:/app/mytrip.db

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - backend
